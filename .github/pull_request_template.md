## Summary

<!-- What does this change and why? Link the issue: Fixes #123 -->

## Testing

<!-- How did you verify it? Screenshots help for UI changes. -->

## Checklist

- [ ] Tests added or updated, `go test -race ./...` passes
- [ ] `gofmt -l .` is empty and `go vet ./...` passes
- [ ] `docs/configuration.md` and `mokey.toml.sample` updated if options changed
- [ ] `ChangeLog.md` updated under `[Unreleased]` if behavior changed
- [ ] New UI strings added to `server/translations/english.toml`
