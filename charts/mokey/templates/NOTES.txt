mokey {{ .Chart.AppVersion }} deployed.

{{- if .Values.ingress.enabled }}
URL: http{{ if .Values.ingress.tls }}s{{ end }}://{{ .Values.ingress.host }}
{{- else }}
Port-forward to access the UI:
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "mokey.fullname" . }} 8866:{{ .Values.service.port }}
{{- end }}

{{- if and (not .Values.existingKeytabSecret) (not .Values.ipa.enroll) }}

WARNING: no keytab source configured. Set existingKeytabSecret (recommended)
or ipa.enroll=true, otherwise the pod will fail to start.
{{- end }}
